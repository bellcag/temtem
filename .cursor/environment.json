{
  "name": "TeMPo Portal",
  "install": "npm ci",
  "terminals": [
    {
      "name": "dev",
      "command": "npm run dev",
      "description": "Vite dev server for the TeMPo prototype on port 5173 (Process v16 at /process-v16?phase=setup). The dev script binds all interfaces so 127.0.0.1 and forwarded ports work."
    }
  ],
  "ports": [
    { "port": 5173 }
  ]
}
